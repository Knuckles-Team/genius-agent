[build-system]
requires = [ "setuptools>=80.9.0", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "genius-agent"
version = "4.0.0"
description = "GeniusAgent Search Engine for Agentic AI!"
readme = "README.md"
classifiers = [ "Development Status :: 5 - Production/Stable", "License :: Public Domain", "Environment :: Console", "Operating System :: POSIX :: Linux", "Programming Language :: Python :: 3",]
requires-python = ">=3.12,<3.15"
dependencies = [ "universal-skills[web-crawler,web-search]", "systems-manager[mcp,agent]", "agent-utilities[agent,logfire]>=2.0.0,<3.0.0",]
[[project.authors]]
name = "Project Maintainers"
email = "maintainers@users.noreply.github.com"

[project.license]
text = "MIT"

[project.scripts]
genius-agent = "genius_agent.agent_server:agent_server"
genius-mcp = "genius_agent.mcp_server:mcp_server"

[project.entry-points."agent_utilities.skill_providers"]
genius-agent = "genius_agent.skills"

[project.entry-points."agent_utilities.ontology_providers"]
genius-agent = "genius_agent.ontology"

[project.entry-points."agent_utilities.source_connector_providers"]
genius-agent = "genius_agent.connectors"

[project.entry-points."agent_utilities.prompt_providers"]
genius-agent = "genius_agent.prompts"

[project.optional-dependencies]
mcp = [ "agent-utilities[mcp]>=2.0.0,<3.0.0",]
agent = [ "agent-utilities[agent,logfire]>=2.0.0,<3.0.0",]
all = [ "agent-utilities[mcp,agent,logfire]>=2.0.0,<3.0.0",]
test = [
    "pytest-xdist>=3.8.0", "pytest>=9.1.1", "pytest-asyncio>=1.4.0", "pytest-cov>=7.1.0",]

[tool.setuptools]
include-package-data = true

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = true

[dependency-groups]
dev = [
    "pytest-timeout>=2.4.0",
]

[tool.setuptools.package-data]
genius_agent = [ "mcp_config.json", "agent_data/**", "prompts/**", "skills/**", "ontology/**", "connectors/**",]

[tool.ruff.lint]
select = [ "E", "F", "I", "UP", "B",]
ignore = [ "E402", "E501", "B008",]

[tool.setuptools.packages.find]
where = [ ".",]

[tool.vulture]
ignore_names = ["request", "config"]
