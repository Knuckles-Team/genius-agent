[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "genius-agent"
version = "2.13.0"
description = "GeniusAgent Search Engine MCP Server for Agentic AI!"
readme = "README.md"
authors = [{ name = "Audel Rouhi", email = "knucklessg1@gmail.com" }]
license = { text = "MIT" }
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "License :: Public Domain",
    "Environment :: Console",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.10"
dependencies = [
    "pydantic-ai-slim[fastmcp,openai,anthropic,google,huggingface,a2a,ag-ui,web]>=1.32.0",
    "pydantic-ai-skills",
    "fastapi>=0.128.0",
    "crawl4ai>=0.8.0",
    "graphiti-core[neo4j,falkordb,kuzu,anthropic,google-genai,openai,groq]>=0.26.3",
    "fastmcp>=3.0.0b1",
    "eunomia-mcp>=0.3.10",
    "mem0-mcp-server>=0.2.1",
]

[project.scripts]
genius-mcp = "genius_agent.genius_mcp:genius_agent_mcp"
genius-agent = "genius_agent.genius_agent:agent_server"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-data]
genius_agent = ["mcp_config.json", "skills/**"]

[tool.setuptools]
include-package-data = true
